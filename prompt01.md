I'd like you to set up a small PMPM based monorable project that will be responsible for generating fancy curriculums. The source of the curriculum will be in a markdown folder. How it should work? So basically, this will be a a a web server built on top of HoNo, h o n o, h t m x, user interface, will set up the source directory. which contains information about your curriculum, your experience as a set of markdown files with a proper front matter. This is currently being built in parallel with another Claude called session, so you don't have to take care about this now, but it will be the source for the generation of the the final final CV. The CV will use a predefined design. First iteration should contain something similar to what is on LinkedIn. The preview output should be a HTML page, but there should be also the possibility to generate a PDF out of it.
