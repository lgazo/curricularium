---
description: Bowser connects to `default` session and finds out what it sees
---
use `bowser` skill to connect to `default` playwright session.
describe what you see on the page and where are you
